9.10进度：
	测试地址：http://localhost:8080/tableclient/dd/analyze.html
	项目共五个：tableclient、work-boom、work-param、work-platform、work-table
	其中tableclient是ext改成的，主要放前台文件，其他的四个中，work-platform是主项目，其他三个是模块项目，打包时只需要打前台和主项目，
	数据建表初始化和数据初始化在svn/br_platfrom_001/sql/mysql/下，运行之后即可
	
9.11问题：
	1、浏览器不兼容，ie中数据视图中的数据刷不出来，需要修改某个变量的值其他的才能出来
	2、在变量视图新建一个变量，然后未保存的状态下在数据视图给该变量赋值，点击保存，刷新后数据视图中的该变量值为空
9.12问题：
	1、excel导入数据库时，excel中的中文不能导入（已解决）
	2、含有下拉框的数据视图不能刷出来，
9.13发现问题：
	1、选择变量类型之后，小数位数不能设置可用不可用，缺失值的类型不能分别设置，所有这一列的editor类型为最后设置的变量类型(解决，事件换成beforeedit)
	2、插入一行时，位置变化，会自动排序
	3、grid的高度不能自适应                         （暂时去除分页，以达到动态适应窗口大小）
	4、点击变量值标签，显示uuid而不是文字
	5、ie中，直接在变量视图中添加新纪录没问题，切换导数据视图，在切换回来时无法添加新纪录(ie中有问题，火狐没事)
9.16发现问题：
	1、修改完表之后左侧树不能刷新
	2、修改完之后刷新页面报表不存在错误 （后台没有对表名进行更新，）
	3、以id为字段名时，添加一行通过geiNewRecords()得不到，得到的是update的记录
9.22问题：
	1、美元设定货币等字段类型在界面表现上不完善，而且format格式化没做
	2、通用表功能没做
	3、拼写检查按钮功能没做；日期格式化不完善
	4、数据视图中，点击单元格，不修改也添加了修改标示